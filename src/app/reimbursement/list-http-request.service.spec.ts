import { TestBed } from '@angular/core/testing';

//import { ListHttpRequestService } from './list-http-request.service';

describe('ListHttpRequestService', () => {
 // let service: ListHttpRequestService;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    //service = TestBed.inject(ListHttpRequestService);
  });

  it('should be created', () => {
   // expect(service).toBeTruthy();
  });
});
